# Optional. If set, every signup is POSTed to this URL as JSON:
#   { "email": "...", "source": "evernav-landing", "ts": "..." }
# Wire it to Zapier / Make / a Discord webhook / a Google Sheet / your own API.
# Leave it unset to just accept signups (logged to the function output).
SIGNUP_WEBHOOK_URL=
