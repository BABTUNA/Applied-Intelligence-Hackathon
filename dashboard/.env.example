# Copy to .env.local and fill in. Re-run `next build` after changing.

# InsForge project URL (the oss_host from .insforge/project.json).
NEXT_PUBLIC_INSFORGE_URL=https://uqi28a23.us-east.insforge.app

# Public anon JWT — NOT the ik_ admin key. Safe to bundle in client code.
NEXT_PUBLIC_INSFORGE_ANON_KEY=eyJ…
